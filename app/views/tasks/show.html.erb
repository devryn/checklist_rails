<h2>Task:</h2>
#<%= task.position %>:
<%= task.body %><p>
Completed? <%= task.completed %>
<%= link_to "Edit", edit_task_path(task), class: "btn btn-default" %>
<%= link_to "Delete", task, class: "btn btn-danger", method: :delete, data: { confirm: "Are you sure you want to delete this task?"} %>
